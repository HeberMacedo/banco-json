{
  "name": "api-tpizza-artesanal",
  "version": "1.0.0",
  "private": false,
  "scripts": {
    "dev": "json-server --watch db.json --host 0.0.0.0 --port 3000",
    "start": "json-server --watch db.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}